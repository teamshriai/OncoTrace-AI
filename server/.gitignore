.venv/
__pycache__/
*.pyc
.pytest_cache/

# Holds the shared demo credential -- never commit. Copy .env.example to .env
# and fill in real values locally.
.env

# Reference databases (SnpEff, ClinVar, CIViC, genome FASTAs) — multi-GB,
# never committed. Populated locally via scripts/download_references.sh.
resources/*
!resources/.gitkeep
